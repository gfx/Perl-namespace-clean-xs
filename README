This is Perl module namespace::clean.

INSTALLATION

namespace::clean installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    $ cpan namespace::clean

Download it, unpack it, then build it as per the usual:

    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

namespace::clean documentation is available as in POD. So you can do:

    $ perldoc namespace::clean

to read the documentation online with your favorite pager.

Goro Fuji (gfx)
